import { CompactWrapper } from './CompactWrapper';
import logo from '../../assets/logo.png';
// import animatedLogo from '../../assets/animated.mp4'; // TODO: Use animated version

interface CompactIdleProps {
    baseNotchWidth: number;
    isHovered: boolean;
    contentOpacity: number;
}

export function CompactIdle({ baseNotchWidth, isHovered, contentOpacity }: CompactIdleProps) {
    return (
        <></>
    );
}
